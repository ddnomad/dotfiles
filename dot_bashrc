# If not running interactively, do nothing
# shellcheck disable=SC1090
[[ $- != *i* ]] && return

# Load "modules"
source ~/.bash/prompt.bash
source ~/.bash/settings.bash
source ~/.shell/aliases.sh
source ~/.shell/env.sh

# Source functions
for func in ~/.bash/functions/*; do
    # shellcheck disable=SC1090
    test -f "${func}" && source "${func}"
done

# FIXME: MacOS specifc temp hax
# TODO: What is this and do I need it?
export GUILE_TLS_CERTIFICATE_DIRECTORY=/usr/local/etc/gnutls/

export PATH="/usr/local/opt/gnu-indent/libexec/gnubin:$PATH"
export PATH="/usr/local/opt/gnu-getopt/bin:$PATH"
export PATH="/usr/local/opt/coreutils/libexec/gnubin:$PATH"
export PATH="/usr/local/opt/findutils/libexec/gnubin:$PATH"
export PATH="/usr/local/opt/gnu-tar/libexec/gnubin:$PATH"
export PATH="/usr/local/opt/gnu-sed/libexec/gnubin:$PATH"

# Base16 Shell integration
# FIXME: Switch to a less retarded way of integration base16 on Linux
# NOTE: Not exactly secure thing to use
# BASE16_SHELL=~/.config/base16-shell/
# test -n "$PS1" && \
#  test -s "$BASE16_SHELL/profile_helper.sh" && \
#  eval "$("$BASE16_SHELL/profile_helper.sh")"
